{
  "name": "graphql-todos",
  "homepage": "https://vgratsilev.github.io/graphql-todos",
  "version": "1.0.0",
  "description": "Test GraphQL Todos",
  "main": "src/index.html",
  "scripts": {
    "start": "parcel src/index.html --open",
    "build": "parcel build src/index.html --no-source-maps --public-url ./",
    "predeploy": "rimraf dist && npm run build",
    "deploy": "gh-pages -d dist"
  },
  "author": "vgratsilev",
  "license": "ISC",
  "dependencies": {
    "parcel-bundler": "^1.12.5",
    "rimraf": "^4.1.1"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0"
  }
}
